[build]
builder = "nixpacks"

[deploy]
# Zero-downtime deployments: Railway keeps the old instance serving traffic
# until the new instance passes this health check, then seamlessly switches.
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
