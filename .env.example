# =============================================================================
# TITM Platform - Frontend Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
# =============================================================================

# --- REQUIRED ---

# Supabase (https://supabase.com/dashboard → Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Server-side Supabase access (same service role key as backend)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# OpenAI (used by journal analysis)
OPENAI_API_KEY=sk-proj-...

# Upstash Redis (required in production for distributed rate limiting)
# Production MUST set these values to avoid per-instance in-memory fallback.
# UPSTASH_REDIS_REST_URL=https://<your-upstash-endpoint>.upstash.io
# UPSTASH_REDIS_REST_TOKEN=<your-upstash-rest-token>

# --- OPTIONAL (defaults shown) ---

# AI Coach backend URL (defaults to http://localhost:3001)
# NEXT_PUBLIC_AI_COACH_API_URL=http://localhost:3001

# Web Push VAPID public key for browser push subscriptions
# NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-base64url-vapid-public-key

# Sentry (error monitoring — leave blank to disable)
# NEXT_PUBLIC_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
# SENTRY_AUTH_TOKEN=sntrys_...
# SENTRY_ORG=your-org-slug
# SENTRY_PROJECT=titm-frontend
